{
  "name": "minecraft-hosting-platform",
  "version": "1.0.0",
  "description": "Scalable Minecraft Hosting Platform with Multi-Host Architecture",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "agent",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:agent\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:agent": "npm run dev --workspace=agent",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
